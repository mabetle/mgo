// package mdb inculde many usefull functions for processing database.
// package entry is mdb.NewSql(sql.DB)
//
// link program example:
// s:=mdb.Sql.NewLinkSql()
// s.Select(columns).From(table).Where(where).Args(args ...).Order(column).Desc(column).DoQuery()
package mdb
